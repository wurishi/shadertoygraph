{"ver":"0.1","info":{"id":"ds3fz8","date":"1714418467","viewed":32,"name":"number count","username":"hyverno","description":"short desc","likes":0,"published":1,"flags":0,"usePreview":0,"tags":["letter"],"hasliked":0,"parentid":"","parentname":""},"renderpass":[{"inputs":[{"id":"4dXGRn","filepath":"/media/a/10eb4fe0ac8a7dc348a2cc282ca5df1759ab8bf680117e4047728100969e7b43.jpg","previewfilepath":"/media/ap/10eb4fe0ac8a7dc348a2cc282ca5df1759ab8bf680117e4047728100969e7b43.jpg","type":"texture","channel":1,"sampler":{"filter":"mipmap","wrap":"repeat","vflip":"true","srgb":"false","internal":"byte"},"published":1},{"id":"4dXGzr","filepath":"/media/a/08b42b43ae9d3c0605da11d0eac86618ea888e62cdd9518ee8b9097488b31560.png","previewfilepath":"/media/ap/08b42b43ae9d3c0605da11d0eac86618ea888e62cdd9518ee8b9097488b31560.png","type":"texture","channel":0,"sampler":{"filter":"mipmap","wrap":"repeat","vflip":"true","srgb":"false","internal":"byte"},"published":1}],"outputs":[{"id":"4dfGRr","channel":0}],"code":"float plot(vec2 st, float pct){\n  return  smoothstep( 1., 1., .0) -\n          smoothstep( 1., 1., .0);\n}\n\n\nvec3 mask(bool b, vec2 uv, float v){\n    float y;\n    //  1/partition / position\n    // y = step(uv.x, 1./3.*3.);\n    if (b) {\n        y = step(v, uv.x);\n    } else {\n        y = step(uv.x, v);\n    }\n    \n    vec3 color = vec3(y);\n    float pct = plot(uv,0.);\n    color = (0.999-pct)*color;\n    \n    return color;\n}\n\n\nvoid mainImage( out vec4 fragColor, in vec2 fragCoord )\n{\n    float number = 6964.0; // Remplacez par la valeur que vous souhaitez afficher\n    float nLength = floor(log(number)/log(10.0));\n    if (nLength < 1.0) nLength = 1.0;\n\n    vec2 textureResolution = vec2(10.0, 1.0);\n\n    vec2 charSize = 1.0 / textureResolution;\n\n    vec2 uv = fragCoord.xy / iResolution.xy;\n\n    vec4 finalColor = vec4(0.);\n\n\n    if (int(nLength) == 1) {\n        vec2 texCoord = vec2(uv.x * 1.0/ 10.0 + float(number) / 10.0, uv.y);\n        finalColor = texture(iChannel0, texCoord);\n\n    } else {\n\n        int at = 1;\n        for (int i = int(nLength); i >= 0; i--) {\n            int digit = int(floor(mod(number / pow(10.0, float(i)), 10.0)));\n            vec2 texCoord = vec2(uv.x * (nLength + 1.0) / 10.0 + (float(digit) - float(at) + 1.) / 10.0, uv.y);\n\n            /*\n                NOTE: Debug le mask\n            */\n\n            // debug\n            // if (i == int(nLength)) {\n            //     // finalColor +=  vec4(mask(false, uv, 1./((nLength + 1.))+(float(at)-1.)/(nLength+1.)),uv) * texture(iChannel0, texCoord);\n            // }  if (i == 0) {\n            //     // finalColor += vec4(mask(true, uv, 1./((nLength + 1.))*float(1)),uv) *  texture(iChannel0, texCoord);\n            //     finalColor += vec4(mask(true, uv, (float(at)-1.)/((nLength + 1.))),uv) * vec4(mask(false, uv, 1./((nLength + 1.))+(float(at)-1.)/(nLength+1.)),uv) *  texture(iChannel0, texCoord);\n            // } else {\n            //     // finalColor += vec4(mask(true, uv, 1./((nLength + 1.))*float(i)),uv) *  texture(iChannel0, texCoord);\n            // }\n\n            finalColor += vec4(mask(true, uv, (float(at)-1.)/((nLength + 1.))),uv) * vec4(mask(false, uv, 1./((nLength + 1.))+(float(at)-1.)/(nLength+1.)),uv) *  texture(iChannel0, texCoord);\n\n\n            // finalColor += vec4(mask(uv, 1./(nLength*float(at)+1.)),uv) *  texture(iChannel0, texCoord);\n            at++;\n        }\n    }\n\n\n\n    // finalColor += vec4(mask(uv, .5),uv) *  texture(iChannel0, vec2(uv.x * (nLength + 1.0) / 10.0 + float(5.) / 10.0, uv.y));\n\n    // Définir la couleur finale dans fragColor\n    fragColor = finalColor;\n}","name":"Image","description":"","type":"image"}]}