{"ver":"0.1","info":{"id":"dtfyRl","date":"1691031478","viewed":488,"name":"simulated cells","username":"linround","description":"shaderbook","likes":2,"published":1,"flags":0,"usePreview":0,"tags":["2d"],"hasliked":0,"parentid":"","parentname":""},"renderpass":[{"inputs":[],"outputs":[{"id":"4dfGRr","channel":0}],"code":"\n#define PI 3.1415926\n#define TWO_PI 6.28318530718\nvec2 random2(vec2 p){\n//    使用点乘和sin函数 实现随机值\n    float dotX = dot(p,vec2(127.1,311.7));\n    float dotY = dot(p,vec2(269.5,183.3));\n    vec2 v = sin(vec2(dotX,dotY))*43758.5453;\n    return fract(v);\n}\nvec3 dynamicGridCell(in vec2 st){\n\n    vec3 color = vec3(0.0);\n//    划分单元格\n    st*=3.0;\n\n    vec2 ist = floor(st);\n    vec2 fst = fract(st);\n    float mDist = 1.0;// 最近的距离\n    vec2 mPoint; // 保留最近的点\n\n//    计算周围单元格中心形成该单元格的距离分布\n    for(int y=-1;y<=1;y++){\n        for (int x = -1;x<=1;x++){\n            vec2 neighbor = vec2(float(x),float(y));\n\n//            计算其邻居的随机中心位置以及自己的中心位置\n            vec2 point = random2(ist+neighbor);\n\n            point = 0.5+0.5*sin(iTime+TWO_PI*point);\n\n//            point 计算的是 某个点的随机位置。加上neighbor即可得到该 网格的中心位置\n//            减去fst 即可得到在某个邻居中中心点到该网格小数部分的距离\n            vec2 diff =neighbor+point-fst;\n            float dist = length(diff);\n            if(dist<mDist){\n                mDist = dist;\n                mPoint = point;\n            }\n        }\n    }\n\n    color = vec3(1.0-mDist);\n\n\n//    如果该点的及其周围八个邻点\n    float pct = 1.-step(.03, mDist);\n\n//    绘制中心\n    color = mix(color,vec3(1.0),pct) ;\n    color.z = mPoint.y*mPoint.x;\n\n\n\n    color.b += step(.99, fst.x) + step(.98, fst.y);\n    return color;\n\n}\n\nvoid mainImage( out vec4 fragColor, in vec2 fragCoord )\n{\n    // Normalized pixel coordinates (from 0 to 1)\n    vec2 uv = fragCoord/iResolution.xy;\n\n    vec3 col = dynamicGridCell(uv);\n    // Time varying pixel color\n    //vec3 col = 0.5 + 0.5*cos(iTime+uv.xyx+vec3(0,2,4));\n\n    // Output to screen\n    fragColor = vec4(col,1.0);\n}\n","name":"Image","description":"","type":"image"}]}