{"ver":"0.1","info":{"id":"McKyzW","date":"1731581088","viewed":32,"name":"exponential collatz fractal","username":"Sirox","description":"done this out of boredom","likes":2,"published":1,"flags":0,"usePreview":0,"tags":["fractal","collatz"],"hasliked":0,"parentid":"","parentname":""},"renderpass":[{"inputs":[],"outputs":[{"id":"4dfGRr","channel":0}],"code":"#define threshold 10000.0\n#define iters 100\nconst float pi = acos(-1.0);\n\nvec2 cmul(vec2 a, vec2 b) {\n    return vec2(a.x * b.x - a.y * b.y, a.y * b.x + a.x * b.y);\n}\n\nvoid mainImage(out vec4 fragColor, in vec2 fragCoord) {\n    vec2 uv = (fragCoord / iResolution.xy) * 2.0 - 1.0;\n    uv *= 5.0;\n    vec2 z = uv;\n\n    for (int i = 0; i < iters; i++) {\n        z = (z / 2.0) + cmul(((2.0 * z + vec2(1.0, 0.0)) / 4.0), (vec2(1.0, 0.0) - (exp(-pi * z.y) * vec2(cos(pi * z.x), sin(pi * z.x)))));\n\n        if (length(z) > threshold) {\n            fragColor = vec4(log2(log2(length(z) / threshold)));\n            fragColor.a = 1.0;\n            return;\n        }\n    }\n\n    fragColor = vec4(0.0);\n}\n","name":"Image","description":"","type":"image"}]}