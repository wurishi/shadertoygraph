{"ver":"0.1","info":{"id":"DslfDf","date":"1689240832","viewed":22,"name":"Jojjes yantra","username":"jruu","description":"asdf","likes":0,"published":1,"flags":0,"usePreview":0,"tags":["experiment"],"hasliked":0,"parentid":"cssfDj","parentname":"jruu 0"},"renderpass":[{"inputs":[],"outputs":[{"id":"4dfGRr","channel":0}],"code":"float ndot(vec2 a, vec2 b ) { return a.x*b.x - a.y*b.y; }\nfloat sdRhombus( in vec2 p, in vec2 b ) \n{\n    p = abs(p);\n    float h = clamp( ndot(b-2.0*p,b)/dot(b,b), -1.0, 1.0 );\n    float d = length( p-0.5*b*vec2(1.0-h,1.0+h) );\n    return d * sign( p.x*b.y + p.y*b.x - b.x*b.y );\n}\n\nfloat sdOctogon( in vec2 p, in float r )\n{\n    const vec3 k = vec3(-0.9238795325, 0.3826834323, 0.4142135623 );\n    p = abs(p);\n    p -= 2.0*min(dot(vec2( k.x,k.y),p),0.0)*vec2( k.x,k.y);\n    p -= 2.0*min(dot(vec2(-k.x,k.y),p),0.0)*vec2(-k.x,k.y);\n    p -= vec2(clamp(p.x, -k.z*r, k.z*r), r);\n    return length(p)*sign(p.y);\n}\n\nfloat sdHexagon( in vec2 p, in float r )\n{\n    const vec3 k = vec3(-0.866025404,0.5,0.577350269);\n    p = abs(p);\n    p -= 2.0*min(dot(k.xy,p),0.0)*k.xy;\n    p -= vec2(clamp(p.x, -k.z*r, k.z*r), r);\n    return length(p)*sign(p.y);\n}\n\nvec3 palette( float t ) {\n    /*vec3 a = vec3(0.5, 0.5, 0.5);\n    vec3 b = vec3(0.5, 0.5, 0.5);\n    vec3 c = vec3(1.0, 1.0, 1.0);\n    vec3 d = vec3(0.263,0.416,0.557);\n    \n    /**/vec3 a = vec3(0.200);\n    vec3 b = vec3(0.271, 0.000, 0.271);\n    vec3 c = vec3(1.078, 1.078, 1.078);\n    vec3 d = vec3(-0.072, 0.108, 0.698);/**/\n\n    return a + b*cos( 6.28318*(c*t+d) );\n}\n\nvoid mainImage( out vec4 fragColor, in vec2 fragCoord )\n{\n    vec2 uv = (fragCoord * 2.0 - iResolution.xy) / iResolution.y;\n    vec2 uv0 = uv;\n    vec3 patternColor = vec3(0.0);\n    \n    //float d1 = 1.0 / length(uv0);\n    float d1 = -1.0 * sdRhombus( uv0, 2.0 * vec2(1.0, 1.5) );\n    d1 -= 1.55;\n    d1 = smoothstep(0.1, 0.11, d1);\n    //d1 *= 0.01;\n    //d1 = pow(d1, 0.8);\n    vec3 dotColor = d1 * vec3(1.0);\n    \n    for (float i = 0.0; i < 3.0; i++) {\n        uv = fract(uv * 2.0) - 0.5;\n        \n        //float d = length(uv) * exp(-length(uv0));\n        float d = sdHexagon(uv, 1.0) * exp(-length(uv0));\n        //float d = sdOctogon(uv, .2) * exp(-length(uv0));\n        //float d0 = sdRhombus( uv0, vec2(1.0, 3.0) );\n        //float d0 = length(uv0) - 0.15;\n        //d = min(d, abs(d0));\n        \n        vec3 col = palette(length(uv0) + i * 0.6 + iTime * 0.2);\n        \n        d = sin(d * 24.0 + iTime ) / 18.0;\n        d = abs(d);\n        \n        d = pow(0.01 / d, 2.0);\n        \n        patternColor += col * d;\n    }\n    \n    fragColor = vec4(max(patternColor, dotColor), 1.0);\n}","name":"Image","description":"","type":"image"}]}