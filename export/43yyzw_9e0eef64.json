{"ver":"0.1","renderpass":[{"outputs":[{"channel":0,"id":"4dfGRr"}],"inputs":[{"channel":0,"type":"texture","id":"4dXGzn","filepath":"/media/a/0c7bf5fe9462d5bffbd11126e82908e39be3ce56220d900f633d58fb432e56f5.png","sampler":{"filter":"mipmap","wrap":"repeat","vflip":"true","srgb":"false","internal":"byte"}}],"code":"// Noise functions from https://gist.github.com/patriciogonzalezvivo/670c22f3966e662d2f83\nfloat mod289(float x) { return x - floor(x * (1.0 / 289.0)) * 289.0; }\nvec4 mod289(vec4 x) { return x - floor(x * (1.0 / 289.0)) * 289.0; }\nvec4 perm(vec4 x) { return mod289(((x * 34.0) + iMouse.x + 0.1) * x); }\nvec3 centerOfMass = vec3(0.0, 0.0, -100.0);\n\nfloat noise(vec3 p) {\n    vec3 a = floor(p * 1.0);\n    vec3 d = p - a;\n    d = d * d * (3.0 - 2.0 * d);\n\n    vec4 b = a.xxyy + vec4(0.0, 1.0, 0.0, 1.0);\n    vec4 k1 = perm(b.xyxy);\n    vec4 k2 = perm(k1.xyxy + b.zzww);\n\n    vec4 c = k2 + a.zzzz;\n    vec4 k3 = perm(c);\n    vec4 k4 = perm(c + 1.0);\n\n    vec4 o1 = fract(k3 * (1.0 / 41.0));\n    vec4 o2 = fract(k4 * (1.0 / 41.0));\n\n    vec4 o3 = o2 * d.z + o1 * (1.0 - d.z);\n    vec2 o4 = o3.yw * d.x + o3.xz * (1.0 - d.x);\n\n    return o4.y * d.y + o4.x * (1.0 - d.y);\n}\n\nfloat fbm(vec3 x) {\n    float v = 0.0;\n    float a = 0.5;\n    vec3 shift = vec3(1000);\n    for (int i = 0; i < 9; ++i) {\n        v += a * noise(x);\n        x = x * 2.0 + shift;\n        a *= 0.5;\n    }\n    return v;\n}\n\nfloat sdf(vec3 point) {\n    float radius = fbm(point * 2.0) * 0.5 - fbm((point + 300.0) * 50.0) / 100.0;\n    float asteroid = length(point - centerOfMass) - radius;\n\n    return asteroid;\n}\n\nfloat rayMarch(vec3 rayOrigin, vec3 rayDir, mat3 rotation) {\n    float distMarched = 0.0;\n\n    // Translate to center of mass before rotation\n    vec3 centerOfMass = centerOfMass;\n\n    for (int i = 0; i < 100; i++) {\n        vec3 translatedPoint = rayOrigin - centerOfMass; // Translate to origin\n        vec3 rotatedPoint = rotation * translatedPoint; // Apply rotation\n        rotatedPoint += centerOfMass; // Translate back to original position\n\n        float dist = sdf(rotatedPoint);\n\n        rayOrigin += rayDir * dist;\n        distMarched += dist;\n\n        if (dist < 0.001 || dist > 100.0) {\n            break;\n        }\n    }\n\n    return distMarched;\n}\n\nvec3 getNormal(vec3 point, mat3 rotation) {\n    vec3 centerOfMass = centerOfMass;\n\n    // Translate to center of mass before rotation\n    vec3 staticPoint = inverse(rotation) * (point - centerOfMass) + centerOfMass;\n\n    float dist = sdf(staticPoint);\n    vec3 norm = dist - vec3(\n        sdf(staticPoint - vec3(0.01, 0.0, 0.0)),\n        sdf(staticPoint - vec3(0.0, 0.01, 0.0)),\n        sdf(staticPoint - vec3(0.0, 0.0, 0.01))\n    );\n    return normalize(rotation * norm);\n}\n\nfloat getShadow(vec3 rayOrigin, vec3 lightDir) {\n    float distMarched = 0.0;\n    float shadow = 1.0;\n\n    for (float i = 0.0; i < 100.0; i++) {\n        float dist = sdf(rayOrigin);\n\n        rayOrigin += lightDir * dist;\n        distMarched += dist;\n        shadow = min(shadow, 32.0 * dist / i);\n\n        if (dist < 0.0001 || dist > 100.0) {\n            break;\n        }\n    }\n\n    return max(shadow, 0.33);\n}\n\nfloat getLight(vec3 point, vec3 cameraPos, mat3 rotation) {\n    vec3 normal = getNormal(point, rotation);\n    vec3 lightDir = normalize(vec3(cos(iMouse.x), sin(iMouse.y), 0.0));\n    float light = getShadow(point + 0.02 * normal, lightDir);\n\n    return clamp(dot(normal, lightDir) * light, 0.1, 1.0);\n}\n\n// Rotation matrices for 3-axis rotation\nmat3 rotationMatrixX(float angle) {\n    return mat3(\n        1.0, 0.0, 0.0,\n        0.0, cos(angle), -sin(angle),\n        0.0, sin(angle), cos(angle)\n    );\n}\n\nmat3 rotationMatrixY(float angle) {\n    return mat3(\n        cos(angle), 0.0, sin(angle),\n        0.0, 1.0, 0.0,\n        -sin(angle), 0.0, cos(angle)\n    );\n}\n\nmat3 rotationMatrixZ(float angle) {\n    return mat3(\n        cos(angle), -sin(angle), 0.0,\n        sin(angle), cos(angle), 0.0,\n        0.0, 0.0, 1.0\n    );\n}\n\nvoid mainImage(out vec4 fragColor, in vec2 fragCoord) {\n    vec2 uv = (fragCoord - 0.5 * iResolution.xy) / iResolution.x;\n\n    vec3 cameraPos = centerOfMass + vec3(0.0, 0.0, -1.35);\n    vec3 rayDir = vec3(uv.x, uv.y, 1.0);\n\n    // Combine rotations for X, Y, and Z axes\n    float angleX = iTime * 0.3;\n    float angleY = iTime * 0.4;\n    float angleZ = iTime * 0.5;\n    mat3 rotation = rotationMatrixZ(angleZ) * rotationMatrixY(angleY) * rotationMatrixX(angleX);\n\n    // Compute the inverse rotation in one step (reverse order of operations)\n    mat3 inverseRotation = inverse(rotation);\n\n    float dist = rayMarch(cameraPos, rayDir, rotation);\n    vec3 point = rayDir * dist + cameraPos;\n\n    vec3 col = vec3(0.0);\n\n    if (dist < 100.0) {\n        col = vec3(getLight(point, cameraPos, inverseRotation));\n    }\n\n    fragColor = vec4(col, 1.0);\n}\n","name":"Image","description":"","type":"image"}],"flags":{"mFlagVR":false,"mFlagWebcam":false,"mFlagSoundInput":false,"mFlagSoundOutput":false,"mFlagKeyboard":false,"mFlagMultipass":false,"mFlagMusicStream":false},"info":{"id":"43yyzw","date":"1736024051","viewed":51,"name":"Asteroid Generator with Rotation","username":"Nanerbeet","description":"Forked from Asteroid Generator, I simply asked Chat Jippity to make the asteroid rotate along all 3-axes.  Click anywhere on the screen to change the seed value.  Now, if anyone knows how to stabilize the surface detail...","likes":3,"published":1,"flags":0,"usePreview":0,"tags":["procedural","asteroid","generation"],"hasliked":0,"parentid":"7sjfWm","parentname":"Asteroid generator"}}