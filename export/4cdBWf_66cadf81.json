{"ver":"0.1","info":{"id":"4cdBWf","date":"1733633379","viewed":38,"name":"示波器","username":"Minami","description":"示波器\n点击屏幕可显示导函数","likes":1,"published":1,"flags":0,"usePreview":0,"tags":["2d"],"hasliked":0,"parentid":"","parentname":""},"renderpass":[{"inputs":[],"outputs":[{"id":"4dfGRr","channel":0}],"code":"#define PI 3.1415926\n\nconst vec3 yaxiscolor = vec3(0.04, 1.0, 0.0);\nconst vec3 xaxiscolor = vec3(0.88, 0.06, 0.06);\nvec3 Grid(in vec2 uv) {\n    vec3 color = vec3(0);\n    vec2 cell = 1. - 2. * abs(fract(uv) - .5);\n\n    // step(a,b) == a<=b\n    color += vec3(step(cell.x, 2.*fwidth(uv.x))) * .3;\n    color += vec3(step(cell.y, 2.*fwidth(uv.y))) * .2;\n    color += vec3(0.1) * step(max(abs(uv.x), abs(uv.y)), 1.0);\n\n    color = mix(color, yaxiscolor, step(abs(uv.x), fwidth(uv.x)));\n    color = mix(color, xaxiscolor, step(abs(uv.y), fwidth(uv.y)));\n\n    return color;\n}\n\n\n#define k 1. //波数\n#define w 2.*PI //角频率\n#define t iTime //时间\n//在此处定义波形 有y和g两个通道（点按屏幕显示导函数）\nvoid def(float x, out float y, out float g) {\n    \n    \n    \n    y = -1.+ 2.*abs(fract(abs(w*t-k*x)) - .5);\n    \n    g =1.- smoothstep(0.,1.,abs(mod(w*t-k*x,2.)-1.));\n       \n}\n\n\nvoid funcPlot(in vec2 uv, inout vec3 color) {\n    float y, g;\n    def(uv.x, y, g);\n\n    float square = clamp(sign(sin(6. * 2. * PI * uv.x)), .0, 1.); //虚线方波\n    color.yz += 1. - smoothstep(0., 4. * fwidth(uv.y), abs(uv.y - y));\n    color.rg += (1. - smoothstep(0., 4. * fwidth(uv.y), abs(uv.y - g)));\n\n\n    if(iMouse.z > 0. ) {\n        float df = dFdx(y) / fwidth(uv.x);\n        color.gb += (1. - smoothstep(0., 4. * fwidth(uv.y), abs(uv.y - df))) * square;\n\n        float dg = dFdx(g) / fwidth(uv.x);\n        color.rg += (1. - smoothstep(0., 4. * fwidth(uv.y), abs(uv.y - dg))) * square;\n    }\n\n}\n\nvec2 fixUV(in vec2 c) {\n    return 3. * (2. * c - iResolution.xy) / min(iResolution.x, iResolution.y);\n\n}\nvoid mainImage(out vec4 fragColor, in vec2 fragCoord) {\n    vec2 uv = fixUV(fragCoord);\n    fragColor = vec4(Grid(uv), 1);\n    funcPlot(uv, fragColor.xyz);\n}","name":"Image","description":"","type":"image"}]}