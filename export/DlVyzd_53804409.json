{"ver":"0.1","info":{"id":"DlVyzd","date":"1700562045","viewed":38,"name":"AUTOWOB","username":"brainsandwich","description":"It's wobbling simply, uving its way around","likes":1,"published":1,"flags":0,"usePreview":0,"tags":["sdf","wobble","uv","circle"],"hasliked":0,"parentid":"","parentname":""},"renderpass":[{"inputs":[],"outputs":[{"id":"4dfGRr","channel":0}],"code":"float sdf_circle(vec2 p, float r)\n{\n    return length(p) - r;\n}\n\nfloat sdf_scene(vec2 p)\n{\n    return sdf_circle(p, 0.0);\n}\n\nvec2 wobble(vec2 uv, int octaves, float bfreq)\n{\n    vec2 sum = vec2(0.0);\n    for (int i = 1; i < octaves + 1; i++)\n    {\n        float gain = (1.0 + 0.1 * sin(0.2 * iTime)) / (8.0 * float(i));\n        float freq = (bfreq + 0.2 * sin(0.1 * iTime)) * float(i);\n        float freq_time = 0.05 * freq + (1.0 + 0.001 * sin(0.01 * iTime));\n        float freq_uv = 1.01 * freq + (1.0 + 0.04 * sin(0.02 * iTime));\n        float dif = 0.23;\n        sum = sum + vec2(\n            uv.x + gain * sin(iTime * freq_time + uv.y * freq_uv),\n            uv.y + (gain + dif) * sin(iTime * (freq_time - dif) + uv.x * (freq_uv - dif))\n        );\n    }\n    return sum;\n}\n\n#define DARK vec3(0.0)\n#define LIGHT vec3(1.0, 0.42, 0.1)\n#define WHIT vec3(1.0)\n\nvoid mainImage( out vec4 fragColor, in vec2 fragCoord )\n{\n    // Normalized pixel coordinates (from 0 to 1)\n    vec2 uv = (fragCoord - iResolution.xy/2.0)/iResolution.yy;\n    vec2 uvn = uv;\n    \n    uv = wobble(uv, 5, 3.14);\n\n    // Output to screen\n    float modctrl = 0.8 + 0.1 * sin(0.094 * iTime + 9.0 * length(uv) * (1.0 + 0.5 * sin(0.09 * iTime)));\n    // fragColor = vec4(smoothstep(0.0499, 0.0501, mod(sdf_scene(uv), modctrl)));\n    \n    float dc = fwidth(modctrl) * 0.5;\n    float modder = smoothstep(0.1-dc, 0.1+dc, mod(sdf_scene(uv), modctrl));\n    vec3 dark = mix(DARK, WHIT, smoothstep(-0.1, 0.1, sin(0.4* iTime)));\n    vec3 light = LIGHT;\n    fragColor = vec4(mix(dark, light, modder), 1.0);\n}","name":"Image","description":"","type":"image"}]}