{"ver":"0.1","info":{"id":"MXlcWH","date":"1726138855","viewed":23,"name":"pendulum lollipop 646732464643","username":"HaleyHalcyon","description":"    eer aeae","likes":0,"published":1,"flags":0,"usePreview":0,"tags":["reerree"],"hasliked":0,"parentid":"mdKXRd","parentname":"swingcoin62584÷966"},"renderpass":[{"inputs":[],"outputs":[{"id":"4dfGRr","channel":0}],"code":"const vec2 FULCRUM = vec2(0., -1.5);\nconst float RADIUS = 1.8;\nconst float STRING = .03;\nconst float COIN_OUT = .4;\nconst float COIN_IN = -.06;\n\nconst float TURN = acos(-1.) * 2.;\n// rotation matrix\n#define ROT(x) mat2x2(cos(x + TURN * vec4(0, 0.25, -0.25, 0)))\n// fade function defined by ken perlin\n//float fade(float t) {\n//  return t * t * t * (t * (t * 6. - 15.) + 10.);\n//}\nvoid mainImage( out vec4 fragColor, in vec2 fragCoord )\n{\n    float t = fract(iTime / 2.5);\n    // Normalized pixel coordinates (from 0 to 1)\n    vec2 uv = (2.*fragCoord - iResolution.xy) / iResolution.y;\n    uv.y *= -1.;\n\n    vec2 uvPend = ((uv + FULCRUM) * ROT(0.3 * cos(t * TURN))) + vec2(0, RADIUS);\n\n    float r = length(uvPend);\n    float a = atan(uvPend.y, uvPend.x) / TURN;\n    \n    vec3 col = mix(\n        mix(\n            mix(\n                vec3(0.7,0.2,0.5),\n                vec3(0.5,0.1,0.4),\n                step(fract(\n                    2.0 * log(r) +\n                    5. * a +\n                    -0.2 * cos(-0.3 + t * TURN)\n                    //+1. * t\n                ), 0.5)\n            ),\n            // string color\n            vec3(0.8),\n            // string\n            step(abs(uvPend.x), STRING)\n            * step(0.9 * COIN_OUT, uvPend.y)\n        ),\n        // coin color\n        mix(\n            vec3(0.98, 0.9, 0.92),\n            vec3(0.9, 0.5, 0.6),\n            step(fract(\n                5.5 * r +\n                2. * a\n            ), 0.5)\n        ),\n        // coin\n        step(r, COIN_OUT) * step(COIN_IN, r)\n    );\n\n    // Output to screen\n    fragColor = vec4(col,1.0);\n}","name":"Image","description":"","type":"image"}]}