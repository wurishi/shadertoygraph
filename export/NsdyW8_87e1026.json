{"ver":"0.1","info":{"id":"NsdyW8","date":"1653399879","viewed":66,"name":"Simple twist ·:)","username":"Alluysl","description":"Simple twist transformation antialiased with supersampling","likes":0,"published":1,"flags":0,"usePreview":0,"tags":["twist"],"hasliked":0,"parentid":"","parentname":""},"renderpass":[{"inputs":[],"outputs":[{"id":"4dfGRr","channel":0}],"code":"vec4 twist(vec2 uv)\n{\n    // Offset UVs\n    uv -= vec2(0.5, 0.5);\n    \n    // Twist UVs\n    float angle = iTime * length(uv);\n    float cAngle = cos(angle);\n    float sAngle = sin(angle);\n    mat2 rotMat = mat2(cAngle, -sAngle, sAngle, cAngle);\n    uv = rotMat * uv;\n    \n    // Make pattern\n    uv += vec2(0.5, 0.5);\n    uv = mod(2.0 * uv, 1.0);\n    uv -= vec2(0.5, 0.5);\n\n    return vec4(\n        uv.x > 0.0 ? 1.0 : 0.0,\n        uv.y > 0.0 ? 1.0 : 0.0,\n        uv.x * uv.y > 0.0 ? 0.5 : 0.0,\n        1.0);\n}\n\n#define AA_DIST 0.5\n\nvoid mainImage( out vec4 fragColor, in vec2 fragCoord )\n{\n    vec4 c0 = twist((fragCoord + vec2(0.5 * AA_DIST, AA_DIST)) / iResolution.xy);\n    vec4 c1 = twist((fragCoord + vec2(-AA_DIST, 0.5 * AA_DIST)) / iResolution.xy);\n    vec4 c2 = twist((fragCoord - vec2(0.5 * AA_DIST, AA_DIST)) / iResolution.xy);\n    vec4 c3 = twist((fragCoord - vec2(-AA_DIST, 0.5 * AA_DIST)) / iResolution.xy);\n    \n    fragColor = mix(mix(c0, c1, 0.5), mix(c2, c3, 0.5), 0.5);\n}","name":"Image","description":"","type":"image"}]}