{"ver":"0.1","info":{"id":"4cSfWG","date":"1725412133","viewed":33,"name":"whirl saw tooth","username":"nahuel36","description":"whirl","likes":0,"published":1,"flags":0,"usePreview":0,"tags":["whirl"],"hasliked":0,"parentid":"","parentname":""},"renderpass":[{"inputs":[],"outputs":[{"id":"4dfGRr","channel":0}],"code":"#define uNumSticks 10.0\n#define uRotationSpeed 0.5\n#define uTwistAmount 10.0\n\nfloat Unity_SawtoothWave_float(float In)\n{\n    return 2.0 * (In - floor(0.5 + In));\n}\n\nvoid mainImage( out vec4 fragColor, in vec2 fragCoord )\n{\n    vec2 uv = fragCoord.xy / iResolution.xy;\n    uv = uv * 2.0 - 1.0;\n    uv.x *= iResolution.x / iResolution.y;\n    \n    float radius = length(uv);\n    float angle = atan(uv.y, uv.x);\n\n    // Apply twisting effect\n    angle += radius * uTwistAmount + iTime * uRotationSpeed;\n\n    // Calculate stick index\n    float stickIndex = mod(angle * uNumSticks / (2.0 * 3.14159265), 1.0);\n\n    // Create stripes\n//  float stripe = step(1.-radius-0.15*sin(radius*25.), stickIndex);\n    float stripe = step(1.-radius-0.15*Unity_SawtoothWave_float(radius*25.), stickIndex);\n\n    // Create the tunnel effect by fading the stripes with distance\n    float tunnel = exp(-radius * 1.75);\n\n    vec3 color = vec3(stripe * tunnel);\n\n    fragColor = vec4(color, 1.0);\n}\n\n","name":"Image","description":"","type":"image"}]}