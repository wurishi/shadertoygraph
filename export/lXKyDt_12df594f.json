{"ver":"0.1","renderpass":[{"outputs":[{"channel":0,"id":"4dfGRr"}],"inputs":[],"code":"// CC-BY\n\nconst float CS = 1.0;\n\n// Scale of the hexagon grid\nfloat S = 15.0 * CS;\n\n// Noise repeat (how many cells on the torus)\nfloat M = 120.0;\n\n// Scale of the noise\nfloat NS = 4.5 * CS;\n\n// Jittering of the vertices in [0.0, 1.0]\nfloat J = 0.02;\n\n// Edge width\nfloat W = 0.08;\n\n// Curviness in [0.0, 1.0]\n// This determines the distance between P0, P1, and P2, P3\n// 0.66 gives good results but 1.0 looks cool as well\n// 0.0 gives straight lines\nfloat C = 0.7;\n\n// Vectors from a center hexagon\n// to all it's neighbors (and neighbors' neighbors)\n// You might need to check for a bigger neighnorhood\n// if the jitter and edge width are both high.\n// Should be at least 1. 0 will only show the start of\n// the edges.\n#define LAYERS 1\n#if (LAYERS == 0)\n# define NEIGH 1\n#elif (LAYERS == 1)\n# define NEIGH 7\n#elif (LAYERS > 1)\n# define NEIGH 19\n#endif\nconst vec2 N_scale = vec2(1.0, sqrt(3.0) / 2.0);\nconst vec2 N[NEIGH] = vec2[NEIGH](\n    // Center\n    vec2(0.0, 0.0)\n    \n#if (NEIGH > 1)\n    ,\n    // First layer\n    vec2( 1.0, 0.0), vec2( 0.5,  1.0), vec2(-0.5,  1.0),\n    vec2(-1.0, 0.0), vec2(-0.5, -1.0), vec2( 0.5, -1.0)\n    \n    \n#if (NEIGH > 7)\n    ,\n    // Second layer\n    vec2( 2.0,  0.0), vec2( 1.5,  1.0), vec2( 1.0,  2.0),\n    vec2( 0.0,  2.0), vec2(-1.0,  2.0), vec2(-1.5,  1.0),\n    vec2(-2.0,  0.0), vec2( 1.5, -1.0), vec2( 1.0, -2.0),\n    vec2( 0.0, -2.0), vec2(-1.0, -2.0), vec2(-1.5, -1.0)\n#endif\n#endif\n);\n\nconst mat2 rot60 = mat2(0.5, sqrt(3.0) / 2.0, -sqrt(3.0) / 2.0, 0.5);\n\nvoid mainImage(out vec4 fragColor, in vec2 fragCoord) {\n    float mouse_clicked = clamp(iMouse.z, 0.0, 1.0);\n    float mouse_y = 2.0 * iMouse.x / iResolution.x - 1.0; \n    vec2 uv = fragCoord / iResolution.y;\n    \n    vec2 time_shift = vec2(iTime);\n    time_shift.x *= iResolution.y / iResolution.x;\n    \n    // Flip the UV, looks a bit better IMO\n    vec2 p = uv.yx * S + time_shift - vec2(2.0, 2.0);\n    vec2 hex_center = hex(p);\n    \n    float min_dot = 1.0;\n    vec3 min_hue = vec3(1.0);\n    for (int i = 0; i < NEIGH; ++i) {\n        vec2 start_cell = hex(hex_center + N_scale * N[i]);\n        vec2 start_point = start_cell + random_in_circle(0.5, start_cell) * J;\n        \n        vec3 start_val = noised(start_cell / S * NS, M);\n        vec2 start_curl = -normalize(start_val.zy) * vec2(1.0, -1.0);\n        \n        vec2 end_cell = hex(start_cell + start_curl);\n        vec2 end_point = end_cell + random_in_circle(0.5, end_cell) * J;\n        \n        vec3 end_val = noised(end_cell / S * NS, M);\n        vec2 end_curl = -normalize(end_val.zy) * vec2(1.0, -1.0);\n        \n        vec2 end_end_cell = hex(end_cell + end_curl);\n        \n        // In case of 2-loop\n        if (end_end_cell == start_cell) {\n            continue;\n        }        \n        \n        vec3 end_end_val = noised(end_end_cell / S * NS, M);\n        \n        // Reduce the amount of steep curves (at the expense of an extra noise call)\n        if ((distance(end_end_cell, start_cell) < 1.1)) {\n            end_cell = end_end_cell;\n            end_point = end_cell + random_in_circle(0.5, end_cell) * J;\n            end_val = end_end_val;\n            end_curl = -normalize(end_val.zy) * vec2(1.0, -1.0);\n        }\n        \n        // Bezier curve control points\n        vec2 p0 = start_point;\n        vec2 p1 = p0 + 0.5 * C * start_curl;\n        vec2 p3 = end_point;\n        vec2 p2 = p3 - 0.5 * C * end_curl;\n        \n        float t = 0.0;\n        vec2 to_line = cubic_bezier(p, p0, p1, p2, p3, t);\n\n        float comparison_dot = dot(to_line, to_line) + 0.007 * sqrt(t) * mouse_clicked;\n        \n        if (comparison_dot < min_dot) {\n            min_dot = comparison_dot;\n            vec3 start_color = normalize(max(abs(hash32(start_cell)), vec3(0.01)));\n            vec3 end_color = normalize(max(abs(hash32(end_cell)), vec3(0.01)));\n            min_hue = mix(start_color, end_color, t);\n        }\n    }\n    \n    float dist = sqrt(min_dot);\n    float w = S / iResolution.y; // For AA, alternative to fwidth(dist)\n    vec3 tree_color = mix(vec3(0.9), min_hue, mouse_clicked);\n    \n    // Background noise\n    vec3 color = vec3((0.5 + 0.5 * noised(p / S * NS, M).x)) * 0.5;\n    \n    // Draw the tree on top with distance field (anti-aliased)\n    color = mix(tree_color, color, smoothstep(-w, w, dist - W));\n\n    fragColor = vec4(pow(color, vec3(1.0 / 2.2)), 1.0);\n}","name":"Image","description":"","type":"image"},{"outputs":[],"inputs":[],"code":"// https://www.shadertoy.com/view/4Xyyzw\nint mod3(int n) {\n    int s = sign(n);\n    return (3 + s * ((s * n) % 3)) % 3;\n}\n\n// Calculate hex ID on a scaled and skewed lattice\nvec2 hex(vec2 p) {\n    vec2 scaled_skewed = vec2(p.x, 0.5 * (p.x + p.y * sqrt(3.0))) * 2.0;\n\n    ivec2 rec_cell = ivec2(floor(scaled_skewed));\n\tvec2 rec_loc = fract(scaled_skewed);\n    \n\tint m3 = mod3(rec_cell.x + rec_cell.y);\n\n    ivec2 hex_id = rec_cell + m3;\n    if (m3 == 2)\n        if (rec_loc.x > rec_loc.y)\n            hex_id -= ivec2(1, 2);\n        else\n            hex_id -= ivec2(2, 1);\n            \n    vec2 hex_id_f = vec2(hex_id);        \n    \n    return vec2(hex_id_f.x, sqrt(3.0) * (2.0 * hex_id_f.y - hex_id_f.x) / 3.0) / 2.0;\n}\n\n// https://www.shadertoy.com/view/4djSRW\nvec2 hash22(vec2 p) {\n    // credit to Dave_Hoskins - https://www.shadertoy.com/view/4djSRW\n\tvec3 p3 = fract(vec3(p.xyx) * vec3(.1031, .1030, .0973));\n    p3 += dot(p3, p3.yzx+33.33);\n    return fract((p3.xx+p3.yz)*p3.zy);\n}\n\n// https://www.shadertoy.com/view/4djSRW\nfloat hash12(vec2 p) {\n\tvec3 p3  = fract(vec3(p.xyx) * .1031);\n    p3 += dot(p3, p3.yzx + 33.33);\n    return fract((p3.x + p3.y) * p3.z);\n}\n\n// https://www.shadertoy.com/view/4djSRW\nvec3 hash32(vec2 p) {\n\tvec3 p3 = fract(vec3(p.xyx) * vec3(.1031, .1030, .0973));\n    p3 += dot(p3, p3.yxz+33.33);\n    return fract((p3.xxy+p3.yzz)*p3.zyx);\n}\n\nvec2 mod_hash(vec2 x, vec2 m) {\n    x = mod(x, m);\n    const vec2 k = vec2(0.3183099, 0.3678794);\n    x = x * k + k.yx;\n    return -1.0 + 2.0 * fract(16.0 * k * fract(x.x * x.y * (x.x + x.y)));\n}\n\nvec3 noised(vec2 p, float m) {\n    vec2 i = floor(p);\n    vec2 f = fract(p);\n\n    vec2 u = f*f*f*(f*(f*6.0-15.0)+10.0);\n    vec2 du = 30.0*f*f*(f*(f-2.0)+1.0);\n\n    vec2 ga = mod_hash(i + vec2(0.0, 0.0), vec2(m));\n    vec2 gb = mod_hash(i + vec2(1.0, 0.0), vec2(m));\n    vec2 gc = mod_hash(i + vec2(0.0, 1.0), vec2(m));\n    vec2 gd = mod_hash(i + vec2(1.0, 1.0), vec2(m));\n\n    float va = dot(ga, f - vec2(0.0, 0.0));\n    float vb = dot(gb, f - vec2(1.0, 0.0));\n    float vc = dot(gc, f - vec2(0.0, 1.0));\n    float vd = dot(gd, f - vec2(1.0, 1.0));\n\n    return vec3(va + u.x*(vb-va) + u.y*(vc-va) + u.x*u.y*(va-vb-vc+vd),\n        \t\tga + u.x*(gb-ga) + u.y*(gc-ga) + u.x*u.y*(ga-gb-gc+gd) +\n        \t\tdu * (u.yx*(va-vb-vc+vd) + vec2(vb,vc) - va));\n}\n\nvec2 random_in_circle(float r, vec2 seed) {\n    float a = hash12(seed);\n    float b = hash12(a * seed);\n    float R = sqrt(a) * r;\n    float A = 6.283 * b;\n    return R * vec2(cos(A), sin(A));\n}\n\nvec2 to_segment(vec2 p, vec2 start, vec2 end) {\n\tvec2 sp = p - start;\n\tvec2 se = end - start;\n\tfloat dist = clamp(dot(sp, se) / dot(se, se), 0.0, 1.0);\n\tvec2 to_line = se * dist - sp;\n\treturn to_line;\n}\n\nvec2 eval_cubic(vec2 a, vec2 b, vec2 c, vec2 d, float t) {\n    return d + t * (c + t * (b + t * a));\n}\n\n#define BEZIER_SEGMENTS 10\n\nvec2 cubic_bezier(vec2 p, vec2 p0, vec2 p1, vec2 p2, vec2 p3, out float t) {\n    // Cubic Bezier polynomial coefficients\n    // B(x)\n    vec2 a = p3 - p0 + 3.0 * (p1 - p2);\n    vec2 b = 3.0 * p0 - 6.0 * p1 + 3.0 * p2;\n    vec2 c = -3.0 * p0 + 3.0 * p1;\n    vec2 d = p0;\n    \n    // Segmented bezier\n    vec2 min_vec = vec2(0.0);\n    float min_dot = 100.0;\n    vec2 last_p = eval_cubic(a, b, c, d, 0.0);\n    for (int i = 1; i < BEZIER_SEGMENTS + 1; ++i) {\n        float next_t = float(i) / float(BEZIER_SEGMENTS);\n        vec2 next_p = eval_cubic(a, b, c, d, next_t);\n        \n        vec2 candidate = to_segment(p, last_p, next_p);\n        float candidate_dot = dot(candidate, candidate);\n        \n        last_p = next_p;\n        \n        if (candidate_dot < min_dot) {\n            min_dot = candidate_dot;\n            min_vec = candidate;\n            t = next_t - 0.5 / float(BEZIER_SEGMENTS);\n        }\n    }\n    \n    return min_vec;\n}","name":"Common","description":"","type":"common"}],"flags":{"mFlagVR":false,"mFlagWebcam":false,"mFlagSoundInput":false,"mFlagSoundOutput":false,"mFlagKeyboard":false,"mFlagMultipass":false,"mFlagMusicStream":false},"info":{"id":"lXKyDt","date":"1736848503","viewed":48,"name":"Bezier loops","username":"mathiasplans","description":"Creates loops along the curls of the noise with cubic Bezier curves connected to each other. The curves are C1 continuous. Hold down mouse to see edge directions. The paths can be quite long. Could not yet figure out how to make everything end in a loop.","likes":8,"published":1,"flags":0,"usePreview":0,"tags":["grid","bezier","graph"],"hasliked":0,"parentid":"4XKcDD","parentname":"Bezier forest"}}