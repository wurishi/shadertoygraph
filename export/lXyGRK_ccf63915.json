{"ver":"0.1","info":{"id":"lXyGRK","date":"1717879293","viewed":59,"name":"Ripple generator (Tutorial)","username":"Muhammad","description":"Ripple generator based on mouse coord","likes":0,"published":1,"flags":0,"usePreview":0,"tags":["tutorial"],"hasliked":0,"parentid":"","parentname":""},"renderpass":[{"inputs":[],"outputs":[{"id":"4dfGRr","channel":0}],"code":"void mainImage( out vec4 fragColor, in vec2 fragCoord )\n{\n    // Normalized pixel coordinates (from 0 to 1)\n    vec2 uv = fragCoord / iResolution.xy;\n    \n    // Normalized mouse coordinates (from 0 to 1)\n    vec2 mouseCoord = (iMouse.xy / iResolution.xy);\n    // getting normalized mouse coord\n    \n    // Translate UV coordinates relative to the mouse position\n    vec2 translatedUv = (uv * 2.0) - 1.0 - (mouseCoord * 2.0 - 1.0); // this line (mouseCoord changes relatively as the translated uv)\n    //so both are on the same page\n    //bringing the uv coordinates to center based on the mouse position ok?\n    \n    // Compute the fade effect based on distance from the mouse position\n    float removingLargeArea = pow(0.2 - length(uv - mouseCoord), 1.2);\n    //ill explain the above after the following complex code line.\n      \n      \n    \n    // Compute the distance for the ring pattern\n    float d = (1.0 - fract(1.0 - length(translatedUv * 4.0) * 2.0 + iTime) * 10.0) * removingLargeArea + removingLargeArea;\n    \n    //after offsetting the center we can change the range of the uv coord\n    // we are making the uv from -4 , 4 i think ok so then , we are calculating length\n    // so we get a ring now since the range is more and we want more rings we use fract\n    // it will make 4 rings. we are doing oneMinus so we can revert the colors , upto you\n    // multiplying it will give the effect making it small cuz we are doing one minus , play with this\n    // now comes for removingLargeArea , since we need them to fade out we need to calculate a gradient based\n    //on mouse coord . it would be a circle as well but of radius that u want a ring to fade out in\n    // we are simply calculating a circle and then multiplying it with the result of d.\n    // follow me on linkedin : https://www.linkedin.com/in/muhammad-anas-1a9824262/\n    // im open for work and learning. I always love to learn! :)\n    \n    \n    \n    // Output to screen\n    fragColor = vec4(vec3(d), 1.0);\n}\n","name":"Image","description":"","type":"image"}]}