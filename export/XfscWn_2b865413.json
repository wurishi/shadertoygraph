{"ver":"0.1","info":{"id":"XfscWn","date":"1721012289","viewed":35,"name":"A tree under the sun","username":"cancanneed","description":"分形","likes":5,"published":1,"flags":0,"usePreview":0,"tags":["fractal","hw5"],"hasliked":0,"parentid":"","parentname":""},"renderpass":[{"inputs":[],"outputs":[{"id":"4dfGRr","channel":0}],"code":"// ref: https://www.shadertoy.com/view/Xds3zM\n\nfloat hash( float n )\n{\n    return fract(sin(n)*114514.4225);\n}\n\nvec2 udSegment( in vec2 p, in vec2 a, in vec2 b )\n{\n\tvec2 pa = p - a, ba = b - a;\n\tfloat h = clamp( dot(pa,ba)/dot(ba,ba), 0.0, 1.0 );\n\treturn vec2( length( pa - ba*h ), h );\n}\n\nvec4 Sun(vec2 uv, vec2 pos, float rad, vec3 color) {\n\tfloat d = length(pos - uv) - rad;\n\tfloat t = clamp(d, 0.0, 1.0);\n\treturn vec4(color, 1.0 - t);\n}\n\nstruct Segment\n{\n\tvec2 p;\n\tfloat a;\n\tfloat l;\n\tfloat wa;\n\tfloat wb;\n\tint level;\n};\n\nSegment stack[16];\n\n    \nvoid mainImage( out vec4 fragColor, in vec2 fragCoord )\n{\n\tvec2 p = (-iResolution.xy + 2.0*fragCoord.xy) / iResolution.y;\n\tvec2 m = vec2(0.0);\n\tif( iMouse.z>0.0 ) m=-1.0 + 2.0*iMouse.xy/ iResolution.xy;\n\t\t\n\n\tstack[0] = Segment( vec2(0.0,-0.85), 0.0, 0.6, 0.13, 0.08, 0 );\n\tint s = 0;\n\t\n\tfloat id = 0.0;\n\tfloat f = 100.0;\n\tfloat g = 100.0;\n\tfor( int i=0; i<84; i++ )\n\t{\n\t\t/// pop from the stack\n\t\tSegment x = stack[s--];\n\t\t\n\t\t// render line\n\t\tvec2 a = x.p;\n\t\tvec2 b = x.p + x.l*vec2(sin(x.a),cos(x.a));\n\t\tvec2 h = udSegment( p, a, b );\n\t\tfloat d = h.x - mix(x.wa,x.wb,h.y);\n\t\tf = min( f,     d);\n\t\tg = min( g, abs(d) );\n\n        // push new stuff into the stack\t\t\n\t\tid += 1.0;\n\t\tif( x.level<6 )\n\t\t{\n\t\t\tfloat an = m.x + 0.5*sin(8.0*x.l+iTime)*x.l;\n\t\t\tfloat a1 = 0.2 + 0.8*hash(3313.115*id) + an;\n\t\t\tfloat a2 = 0.2 + 0.8*hash(1241.506*id) - an;\n\t\t\tfloat l1 = 0.5 + 0.3*hash(5241.343*id);\n\t\t\tfloat l2 = 0.5 + 0.3*hash(9741.241*id);\n            // push\n\t\t\tstack[++s] = Segment( b, x.a+a1, x.l*l1, x.wb, x.wb*0.65, x.level+1 );\n\t\t\tstack[++s] = Segment( b, x.a-a2, x.l*l2, x.wb, x.wb*0.65, x.level+1 );\n\t\t}\n\t}\n    \n    vec3 colorA = vec3(0.720,0.912,0.724);\n    vec3 colorB = vec3(1.000,0.354,0.287);\n    vec2 st = fragCoord.xy/iResolution.xy;\n    vec3 col = vec3(0.0);\n    vec3 pct = vec3(1.0 - st.y);\n    col = mix(colorA, colorB, sin(iTime)*cos(pct));\n    // final color\n\t// col = col + 0.2*vec3( f );\n    // col += 0.03*sin( 180.0*f );\n\tcol = mix( vec3(1.0,0.6,0.3), col, smoothstep( 0.0, 0.005, f ));\n    vec4 color = vec4(col,1.0);\n    vec2 uv = gl_FragCoord.xy;\n\tvec2 center = vec2(iResolution.x * 0.95, iResolution.y * 0.95);\n\tfloat radius = 0.2 * iResolution.y;\n    vec3 colorSun = vec3(0.985,0.478,0.544);\n    vec4 Colorsun = Sun(uv, center, radius, colorSun);\n\tfragColor = mix(color, Colorsun, Colorsun.a);\n    \n}","name":"Image","description":"","type":"image"}]}