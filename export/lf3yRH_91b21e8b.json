{"ver":"0.1","info":{"id":"lf3yRH","date":"1730230133","viewed":30,"name":"z² + c²/z  zoom 2 loop","username":"Leolsssspe","description":"math stuff","likes":0,"published":1,"flags":0,"usePreview":0,"tags":["fractal","complexnumbers"],"hasliked":0,"parentid":"XXBfD3","parentname":"z² + c²/z set zoom 2"},"renderpass":[{"inputs":[],"outputs":[{"id":"4dfGRr","channel":0}],"code":"vec2 multiply(vec2 a, vec2 b)\n{\n    return(vec2(a.x * b.x - a.y * b.y, a.x * b.y + a.y * b.x));\n}\n\nvec2 divide(vec2 a, vec2 b)\n{\n    return(vec2((a.x * b.x + a.y * b.y) / (b.x * b.x + b.y * b.y), (a.y * b.x - a.x * b.y) / (b.x * b.x + b. y * b.y)));\n}\n\nvoid mainImage( out vec4 fragColor, in vec2 fragCoord )\n{\n    vec2 uv = (2.0 * fragCoord - iResolution.xy) / iResolution.y;\n    uv /= exp(8.0 + 7.0 * sin(iTime / 5.0));\n    uv += vec2(-0.415351, -0.0310981);\n    \n    vec2 z = uv;\n    vec3 col = vec3(0.0);\n    \n\n    for(int i = 0; i < 100; i++)\n    {\n        z = multiply(z, z) + divide (multiply(uv, uv), z);\n        if(length(z) > 1000.0)\n        {\n            col = vec3(0.0, 1.0, 1.0);\n            break;\n        }\n    }\n       \n    fragColor = vec4(col, 1.0);\n}","name":"Image","description":"","type":"image"}]}