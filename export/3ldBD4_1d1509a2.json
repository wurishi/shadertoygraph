{"ver":"0.1","info":{"id":"3ldBD4","date":"1612755018","viewed":121,"name":"Fork Simple Sky benjaminf 752","username":"benjaminf","description":"A basic sky and sun shader. Use the mouse for sunPosition","likes":2,"published":1,"flags":0,"usePreview":0,"tags":["simple","sun","sky","orange","shader","scattering","blue","skycolor","horizon","scatter"],"hasliked":0,"parentid":"MsVSWt","parentname":"Simple Sky Shader"},"renderpass":[{"inputs":[],"outputs":[{"id":"4dfGRr","channel":0}],"code":"vec3 getSky(vec2 uv)\n{\n    float atmosphere = sqrt(1.0-uv.y);\n    vec3 skyColor = vec3(0.2,0.2,0.3);\n    \n    float scatter = pow(iMouse.y / iResolution.y,1.0 / 15.0);\n    scatter = 1.0 - clamp(scatter,0.8,1.0);\n    \n    vec3 scatterColor = mix(vec3(1.0),vec3(0.0,0.15,0.2) * 1.5,scatter);\n    return mix(skyColor,vec3(scatterColor),atmosphere / 1.3);\n    \n}\n\nvec3 getSun(vec2 uv){\n\tfloat sun = 1.0 - distance(uv,iMouse.xy / iResolution.y);\n    sun = clamp(sun,0.0,1.0);\n    \n    float glow = sun;\n    glow = clamp(glow,0.0,1.0);\n    \n    sun = pow(sun,100.0);\n    sun *= 100.0;\n    sun = clamp(sun,0.0,1.0);\n    \n    glow = pow(glow,6.0) * 1.0;\n    glow = pow(glow,(uv.y));\n    glow = clamp(glow,0.0,1.0);\n    \n    sun *= pow(dot(uv.y, uv.y), 1.0 / 1.65);\n    \n    glow *= pow(dot(uv.y, uv.y), 1.0 / 2.0);\n    \n    sun += glow;\n    \n    vec3 sunColor = vec3(0.1,0.1,0.1) * sun;\n    \n    return vec3(sunColor);\n}\n\nvoid mainImage( out vec4 fragColor, in vec2 fragCoord )\n{\n\tvec2 uv = fragCoord.xy / iResolution.y;\n    \n    vec3 sky = getSky(uv);\n    vec3 sun = getSun(uv);\n    \n\tfragColor = vec4(sky + sun,1.0);\n}","name":"Image","description":"","type":"image"}]}