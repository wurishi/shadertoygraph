{"ver":"0.1","info":{"id":"tdtBD8","date":"1605019954","viewed":137,"name":"Blurtney Smears","username":"Farbs","description":"Mips the video to heck, then picks one of three nearby samples based on how close it is to the source.","likes":4,"published":1,"flags":0,"usePreview":0,"tags":["filter"],"hasliked":0,"parentid":"","parentname":""},"renderpass":[{"inputs":[{"id":"Xdf3Rn","filepath":"/media/a/e81e818ac76a8983d746784b423178ee9f6cdcdf7f8e8d719341a6fe2d2ab303.webm","previewfilepath":"/media/ap/e81e818ac76a8983d746784b423178ee9f6cdcdf7f8e8d719341a6fe2d2ab303.webm","type":"video","channel":0,"sampler":{"filter":"mipmap","wrap":"clamp","vflip":"true","srgb":"false","internal":"byte"},"published":1}],"outputs":[{"id":"4dfGRr","channel":0}],"code":"vec2 gridSize=vec2(16.0,9.0)*1.0;\nfloat lod = 6.0;\n\nfloat error(vec3 a, vec3 b, vec2 uv1, vec2 uv2)\n{\n    return dot(abs(a-b),vec3(1.0))+dot(abs(uv1-uv2),gridSize)*0.0;\n}\n\nvoid mainImage( out vec4 fragColor, in vec2 fragCoord )\n{\n    // Normalized pixel coordinates (from 0 to 1)\n    vec2 uv = fragCoord/iResolution.xy;\n\n    // target sample\n    vec3 target = texture(iChannel0,uv).rgb;\n\n    float t=iTime*0.0;\n    //vec2 gr=(ceil(uv*gridSize-0.5)+vec2(sin(t),cos(t))*0.75)/gridSize;\t// grid root\n    vec2 gr=uv;\n    \n    // middle\n    vec3 test = textureLod(iChannel0,gr,lod).rgb;\n    float e=error(target, test, uv, gr);\n    float me = e;\t// min error\n    fragColor.rgb = test;\n    \n    // top left\n    vec2 uvt = gr+vec2(-1.0,-1.0)/gridSize;\n/*    test = textureLod(iChannel0,uvt,lod).rgb;\n    e=error(target, test, uv, uvt);\n    fragColor.rgb = e<me ? test : fragColor.rgb;\n    me=min(e,me);*/\n    \n    // top\n/*    uvt = gr+vec2(0.0,-1.0)/gridSize;\n    test = textureLod(iChannel0,uvt,lod).rgb;\n    e=error(target, test, uv, uvt);\n    fragColor.rgb = e<me ? test : fragColor.rgb;\n    me=min(e,me);*/\n    \n    // top right\n/*    uvt = gr+vec2(1.0,-1.0)/gridSize;\n    test = textureLod(iChannel0,uvt,lod).rgb;\n    e=error(target, test, uv, uvt);\n    fragColor.rgb = e<me ? test : fragColor.rgb;\n    me=min(e,me);*/\n    \n    // left\n    uvt = gr+vec2(-1.0,0.0)/gridSize;\n    test = textureLod(iChannel0,uvt,lod).rgb;\n    e=error(target, test, uv, uvt);\n    fragColor.rgb = e<me ? test : fragColor.rgb;\n    me=min(e,me);\n    \n    // right\n    uvt = gr+vec2(1.0,0.0)/gridSize;\n    test = textureLod(iChannel0,uvt,lod).rgb;\n    e=error(target, test, uv, uvt);\n    fragColor.rgb = e<me ? test : fragColor.rgb;\n    me=min(e,me);\n    \n    // bottom left\n/*    uvt = gr+vec2(-1.0,1.0)/gridSize;\n    test = textureLod(iChannel0,uvt,lod).rgb;\n    e=error(target, test, uv, uvt);\n    fragColor.rgb = e<me ? test : fragColor.rgb;\n    me=min(e,me);*/\n    \n    // bottom\n/*    uvt = gr+vec2(0.0,1.0)/gridSize;\n    test = textureLod(iChannel0,uvt,lod).rgb;\n    e=error(target, test, uv, uvt);\n    fragColor.rgb = e<me ? test : fragColor.rgb;\n    me=min(e,me);*/\n    \n    // bottom right\n/*    uvt = gr+vec2(1.0,1.0)/gridSize;\n    test = textureLod(iChannel0,uvt,lod).rgb;\n    e=error(target, test, uv, uvt);\n    fragColor.rgb = e<me ? test : fragColor.rgb;\n    me=min(e,me);*/   \n}","name":"Image","description":"","type":"image"}]}